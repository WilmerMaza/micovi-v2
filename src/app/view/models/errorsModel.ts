export interface Error {
    error?: string; // Esto representa el mensaje de error del servidor
    message?: string; // Esto podría ser un mensaje adicional que proporciona más información sobre el error
    status?: number;
  }
